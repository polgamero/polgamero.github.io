QA 23.21.6 HF23.3.8 — TOURNAMENT RECOVERY + BRACKET

MANDATORY OWNER SMOKE
A) MULTI-TAB SAFETY
1. Tab A: start a Tournament match and reach gameplay.
2. Tab B: same account -> open Tournament.
3. Expected: Tab B shows the active-match safety banner; it does NOT eliminate the run and has no Play button.
4. Continue Tab A; match remains playable.

B) NORMAL LOSS / WIN
1. Finish a rewarded round normally.
2. Expected: settlement message resolves; return-to-fixture enables; receipt/reward exactly once.
3. Repeat with win and loss; loss consolation remains configured (15/30/60/100 by round).

C) SETTLEMENT RETRY
1. Simulate/observe a settlement network failure after terminal state.
2. F5 same tab.
3. Expected: pending settlement retry runs before any interruption path.
4. If still offline, fixture shows Reintentar guardado and does NOT offer interruption resolution for that same pending match.
5. Restore connection and retry; expected exactly one tournamentReceipt/reward.

D) TRUE SAME-TAB INTERRUPTION
1. Start a match, F5 before terminal state.
2. Expected: same reloaded tab resolves that activeMatch as interrupted and tournament is eliminated.

E) ORPHAN MANUAL RECOVERY
1. Open a run with server activeMatch but no local marker (e.g. old/crashed tab).
2. Expected: read-only fixture warns that another tab/device may still own it.
3. Manual Resolve requires confirmation.

F) VISUAL
- Left: 4 Octavos -> 2 Cuartos -> 1 Semi.
- Right mirrors it.
- Final + Copa Argentinia centered.
- Final points/packs visually under cup.
- 48px circular avatars, name below each avatar.
- Desktop mouse drag pans bracket; touch scroll remains usable.
- No excessive vertical fixture scroll at normal desktop viewport.

AUTOMATED LOCAL
- New contract: tools/test_tournament_multitab_bracket_hf23_3_8.mjs
- Game Text contract includes recovery/fixture strings.
- Existing HF12 Tournament bot lifecycle, HF22 Tournament social/mobile contract and game lifecycle must remain green.
