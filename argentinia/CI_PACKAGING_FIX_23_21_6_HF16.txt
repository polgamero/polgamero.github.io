ARGENTINIA 23.21.6 HF16 — CI PACKAGING FIX

Cause of GitHub Actions failure after all functional gates were green:
- regression_legacy_23_17_3_1.zip was stale for TWO intentionally updated live tests:
  * test_audio_manager_23_13_63.mjs
  * test_coin_ui_audio_23_13_65.mjs
- GitHub CI reported only the first drift because the historical archive validator aborted on first mismatch.

Fix:
1. Historical regression archive rebuilt from the complete ci_regression_manifest_23_17_3_1.txt set (62 files), so every archived byte equals the current live test.
2. Regression archive CI validation now COLLECTS ALL missing/drift/extra entries and reports them together before failing.
3. pages.yml no longer hardcodes canonical test count 135 in two independent places. The authoritative count is CANONICAL_FAST_CONTRACTS in SOURCE_SNAPSHOT_MANIFEST_23_21_6.txt; the workflow validates that value and compares the canonical manifest against it.
4. Full GitHub Pages build preflight was executed locally through every repository-owned build step before repackaging.

No gameplay, card balance, Functions source, Firestore Rules, protocol, schema, pool, or callable-count change.
This is a packaging/CI synchronization correction to HF16, not HF17.
